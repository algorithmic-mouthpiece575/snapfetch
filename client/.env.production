# Production build config (VITE_* values are PUBLIC — embedded in the bundle).

# Backend origin: the frontend (snapfetch.harden-tech.com) calls the API here.
VITE_API_ORIGIN=https://snapfetch-server.duckdns.org

# GitHub repo for the "star the project" button + star counter.
VITE_GITHUB_REPO=Zeh-Eox/snapfetch
