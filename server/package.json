{
  "name": "snapfetch-server",
  "version": "0.1.0",
  "private": true,
  "description": "SnapFetch backend — media resolver & downloader (TikTok, Twitter/X, Instagram, YouTube)",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "lint": "tsc --noEmit",
    "test": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "express-rate-limit": "^8.0.1",
    "zod": "^4.0.5"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/node": "^24.12.3",
    "tsx": "^4.20.3",
    "typescript": "~6.0.2"
  }
}
